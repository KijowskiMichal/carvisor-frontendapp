import { SortSafetyPointsPipe } from './sort-safety-points.pipe';

describe('SortSafetyPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSafetyPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
