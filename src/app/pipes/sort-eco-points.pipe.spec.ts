import { SortEcoPointsPipe } from './sort-eco-points.pipe';

describe('SortEcoPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortEcoPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
